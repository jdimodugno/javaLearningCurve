spring:
  application:
    name: learningcurve
  datasource:
    url: jdbc:postgresql://localhost:5432/learningcurve
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server: 8080
